export interface IAnimals {
  id: string;
  name: string;
  breed: string;
  species: string;
  description: string;
  image: string;
  lastLocation: string;
  lastDate: string;
  found: boolean;
}
export interface IAnimalsFound {
  id: string;
  found: boolean;
}
